import 'package:flutter/material.dart';

class TimeSheetAdapter extends StatelessWidget {
  const TimeSheetAdapter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
